{
  "name": "cnt-font-changer",
  "version": "4.0.0",
  "private": true,
  "description": "CNT Chrome extension validation scripts",
  "scripts": {
    "check": "node --check shared.js && node --check background.js && node --check content.js && node --check popup.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json', 'utf8'))\"",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
